#!/usr/bin/env bash

if [[ "${NEURONIX_IDLE_BACKEND:-swayidle}" == "swayidle" ]] && command -v swayidle >/dev/null 2>&1; then
  swayidle timeout 300 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on' &
  swaylock
  kill %%
  exit 0
fi

swaylock
